apiVersion: v1
kind: Service
metadata:
  name: spring-service
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8083 # ← changé ici aussi
      protocol: TCP
  selector:
    app: spring-app
